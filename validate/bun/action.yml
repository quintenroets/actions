name: Validate
description: Validate

runs:
  using: composite
  steps:
    - name: Setup project
      uses: quintenroets/actions/setup/bun@main

    - name: Validate
      shell: bash
      run: make -s validate
